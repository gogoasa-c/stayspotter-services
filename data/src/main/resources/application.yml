spring:
  application:
    name: data
  datasource:
    url: jdbc:postgresql://localhost:5432/ss_db
    username: ss_user
    password: ss_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


server:
  port: 8087
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    hostname: localhost

